 <!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Analyze Page</title>
  <link rel="stylesheet" href="analyze.css" />

  <script src="/script.js" defer></script>
</head>
<body>
  <nav class="navbar">
    <div class="navbar-brand"><i class="fas fa-feather-alt"></i><a href="/">Newsie</a></div>
    <ul class="navbar-links">
      <li><a href="/analyze">Analyze</a></li>
      <li><a href="/stories">Stories</a></li>
    </ul>
  </nav>

  <div class="container">
    <div class="section-title">📝 Add an Article</div>
    <form id="createForm">
      <label for="title">Title</label>
      <input type="text" name="title" required>

      <label for="readingLevel">Reading Level</label>
      <input type="number" name="readingLevel" required>

      <label for="link">Link</label>
      <input type="text" name="link" required>

      <label for="text">Text</label>
      <input type="text" name="text" required>

      <button type="submit">Add Article</button>
    </form>

    <div class="section-title">📚 Articles</div>
    <% for (let i = 0; i < articles.length; i++) { %>
      <div class="card">
        <form onsubmit="updateArticle(event, '<%= articles[i]._id %>')">
          <input name="title" value="<%= articles[i].title %>" required />
          <input name="readingLevel" type="number" value="<%= articles[i].readingLevel %>" required />
          <input name="link" value="<%= articles[i].link %>" required />
          <input name="text" value="<%= articles[i].text %>" required />
          <button type="submit">Update</button>
        </form>
        <button onclick="deleteArticle('<%= articles[i]._id %>')" style="margin-top: 10px; background-color: #ff4d4d; color: white;">Delete</button>
      </div>
    <% } %>

    <div class="section-title">✨ Simplify Article</div>
    <div class="main-content">
      <label for="linkInput">LINK</label>
      <input type="text" id="linkInput" placeholder="Insert link" />

      <label for="readingLevel">Reading Level</label>
      <input type="text" id="readingLevel" placeholder="e.g. Grade 8" />

      <button id="simplifyBtn">Simplify</button>
    </div>

    <div class="sidebar">
      <h3><i class="fas fa-magic"></i> Goes into ChatGPT</h3>
      <div class="chat-box">
        <p>Text...</p>
        <p>Text...</p>
        <p>Text...</p>
      </div>
    </div>
  </div>
</body>
</html>
